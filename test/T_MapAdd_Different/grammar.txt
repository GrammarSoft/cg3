MAPPING-PREFIX = @ ;
DELIMITERS = "<$.>" ;

LIST ASet = ".atc[hm]{2}.*"r ;

SECTION

ADD ($tag) ASet ;
ADD (£tag) ASet ;
ADD (@tag) ASet ;

# Same again, but since the tags already exist in the reading they will not be added again.
ADD ($tag) ASet ;
ADD (£tag) ASet ;
ADD (@tag) ASet ;

MAP (@mapped) (£tag) ;
