MAPPING-PREFIX = £ ;
DELIMITERS = "<$.>" ;

BEFORE-SECTIONS

ADD ($tag) ("word") ;
ADD (§tag) ("word") ;
ADD (£tag) ("word") ;
ADD (@tag) ("word") ;

UNMAP:normal ("word2") ;
UNMAP:unsafe UNSAFE ("word2") ;

MAP (£re-mapped) ("word2") ;
