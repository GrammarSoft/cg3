
SETPARENT
	ON (target) IF (context tests)
	TO (contextual target) IF (context tests)
	;

SETPARENT
	ON (@child) IF (1C (N))
	TO (-1* (@parent)) IF (NOT c (waffle))
	;

-------------------------------------------------

SETCHILD
	ON (target) IF (context tests)
	TO (contextual target) IF (context tests)
	;

SETCHILD
	ON (@parent) IF (1C (N))
	TO (-1* (@child)) IF (c (waffle))
	;
